---
# Tests `#set text(...)` and `#set par(...)`

title: Text Style (font/fontSize/justify)
author: mdtypst test suite
paper: a4
font: Libertinus Serif
fontSize: 10pt
justify: false
---

# Text style

## What is being tested

- `font` and `fontSize` map to `#set text(font: ..., size: ...)`.
- `justify: false` maps to `#set par(justify: false)`.

A paragraph to observe alignment.
