# Template include marker

This verifies that a Typst template file is actually included and executed.
The sidecar preamble references a variable defined by the template.

## Body

If the include fails, compilation should fail.
