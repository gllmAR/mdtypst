# Inline Code

Use `const` instead of `var` for constants.

The `Array.prototype.map()` method creates a new array.

Call `console.log('debug')` for debugging.

Functions are defined with `function name() {}` or `const name = () => {}`.
