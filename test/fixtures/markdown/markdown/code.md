# Code

## Inline Code

Use `npm install` to install dependencies.

The `Array.prototype.map()` method is useful.

## Fenced Code Blocks

```javascript
function greet(name) {
  return `Hello, ${name}!`;
}
```

```python
def greet(name):
    return f"Hello, {name}!"
```

## Indented Code Blocks

    function indented() {
        return 'This is indented with 4 spaces';
    }

## Code Without Language

```
Plain code block
without language specification
```
