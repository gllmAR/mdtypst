# Font Family Test

This is regular text.

This is **bold**, this is *italic*, and this is ***bold italic***.

Inline code should use a mono font: `const x = 1`.

```text
Code blocks should use a dedicated mono font.
```
