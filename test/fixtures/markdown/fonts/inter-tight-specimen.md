---
title: Typographie — Inter Tight
# Load a custom font family at runtime (browser fetch + cache).
# The Typst runtime will register the family so `#set text(font: "Inter Tight")` works.
fontUrls: test/fixtures/fonts/InterTight-wght.ttf
---

test